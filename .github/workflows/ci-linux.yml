name: CI - Linux

on:
  push:
    branches:
      - main
    paths:
      - 'Cabrankengine/**'
      - 'Common/**'
      - 'Sandbox/**'
      - 'UnitTests/**'
      - 'CBKAssetConverter/**'
      - '.github/workflows/ci-linux.yml'

# Pinned LunarG Vulkan SDK version (Linux default renderer). Bump here to update
# every job's download URL and cache key in one place.
env:
  VULKAN_SDK_VERSION: "1.4.350.1"

jobs:
  # -----------------------------------------------------------------------
  # Detect which projects have changed
  # -----------------------------------------------------------------------
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      engine:    ${{ steps.filter.outputs.engine }}
      sandbox:   ${{ steps.filter.outputs.sandbox }}
      converter: ${{ steps.filter.outputs.converter }}
      tests:     ${{ steps.filter.outputs.tests }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            engine:
              - 'Cabrankengine/**'
              - 'Common/**'
              - '.github/workflows/ci-linux.yml'
            sandbox:
              - 'Sandbox/**'
              - 'Cabrankengine/**'
              - 'Common/**'
              - '.github/workflows/ci-linux.yml'
            converter:
              - 'CBKAssetConverter/**'
              - 'Common/**'
              - '.github/workflows/ci-linux.yml'
            tests:
              - 'UnitTests/**'
              - 'Cabrankengine/**'
              - 'Common/**'
              - '.github/workflows/ci-linux.yml'

  # -----------------------------------------------------------------------
  # Engine vendor libraries (GLFW, glad, ImGui, FreeType)
  # Keyed on vendor source only — a src-only engine change never triggers a rebuild
  # -----------------------------------------------------------------------
  build-engine-vendors:
    name: Build engine vendor libs (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            libx11-dev libxrandr-dev libxi-dev libxxf86vm-dev \
            libxcursor-dev libxinerama-dev libgl1-mesa-dev

      - name: Cache premake5
        id: premake-cache
        uses: actions/cache@v4
        with:
          key: premake5-5.0.0-beta8-linux
          path: ./premake5

      - name: Download premake5
        if: steps.premake-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz | tar -xz -C .
          chmod +x ./premake5

      - name: Cache engine vendor libs
        id: vendor-cache
        uses: actions/cache@v4
        with:
          key: engine-vendors-release-linux-${{ hashFiles('Cabrankengine/vendor/GLFW/**', 'Cabrankengine/vendor/glad/**', 'Cabrankengine/vendor/imgui/**', 'Cabrankengine/vendor/freetype/**') }}
          path: |
            bin/Release-linux-x86_64/GLFW
            bin/Release-linux-x86_64/glad
            bin/Release-linux-x86_64/ImGui
            bin/Release-linux-x86_64/FreeType
            bin-int/Release-linux-x86_64/GLFW
            bin-int/Release-linux-x86_64/glad
            bin-int/Release-linux-x86_64/ImGui
            bin-int/Release-linux-x86_64/FreeType

      - name: Build vendor libs
        if: steps.vendor-cache.outputs.cache-hit != 'true'
        # Renderer-agnostic vendor libs: --renderer=opengl skips the Vulkan SDK
        # requirement in premake5.lua (the SDK is only needed to compile the
        # engine's Vulkan backend, not these libraries).
        run: ./premake5 gmake --renderer=opengl && make GLFW glad ImGui FreeType config=release -j$(nproc)

  # -----------------------------------------------------------------------
  # Assimp — keyed on its own source, independent of CBKAssetConverter
  # -----------------------------------------------------------------------
  build-assimp:
    name: Build Assimp (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y zlib1g-dev

      - name: Cache premake5
        id: premake-cache
        uses: actions/cache@v4
        with:
          key: premake5-5.0.0-beta8-linux
          path: ./premake5

      - name: Download premake5
        if: steps.premake-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz | tar -xz -C .
          chmod +x ./premake5

      - name: Cache Assimp
        id: assimp-cache
        uses: actions/cache@v4
        with:
          key: assimp-release-linux-${{ hashFiles('CBKAssetConverter/vendor/assimp/**') }}
          path: |
            bin/Release-linux-x86_64/Assimp
            bin-int/Release-linux-x86_64/Assimp

      - name: Build Assimp
        if: steps.assimp-cache.outputs.cache-hit != 'true'
        # Renderer-agnostic: --renderer=opengl avoids the Vulkan SDK requirement.
        run: ./premake5 gmake --renderer=opengl && make Assimp config=release -j$(nproc)

  # -----------------------------------------------------------------------
  # Cabrankengine
  # -----------------------------------------------------------------------
  build-cabrankengine:
    name: Build Cabrankengine (Release)
    needs: [changes, build-engine-vendors]
    if: needs.changes.outputs.engine == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            libx11-dev libxrandr-dev libxi-dev libxxf86vm-dev \
            libxcursor-dev libxinerama-dev libgl1-mesa-dev

      - name: Cache premake5
        id: premake-cache
        uses: actions/cache@v4
        with:
          key: premake5-5.0.0-beta8-linux
          path: ./premake5

      - name: Download premake5
        if: steps.premake-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz | tar -xz -C .
          chmod +x ./premake5

      - name: Restore engine vendor libs
        uses: actions/cache/restore@v4
        with:
          key: engine-vendors-release-linux-${{ hashFiles('Cabrankengine/vendor/GLFW/**', 'Cabrankengine/vendor/glad/**', 'Cabrankengine/vendor/imgui/**', 'Cabrankengine/vendor/freetype/**') }}
          path: |
            bin/Release-linux-x86_64/GLFW
            bin/Release-linux-x86_64/glad
            bin/Release-linux-x86_64/ImGui
            bin/Release-linux-x86_64/FreeType
            bin-int/Release-linux-x86_64/GLFW
            bin-int/Release-linux-x86_64/glad
            bin-int/Release-linux-x86_64/ImGui
            bin-int/Release-linux-x86_64/FreeType

      - name: Cache engine build outputs
        id: engine-cache
        uses: actions/cache@v4
        with:
          key: engine-release-linux-${{ hashFiles('Cabrankengine/src/**', 'Common/**') }}
          path: |
            bin/Release-linux-x86_64/Cabrankengine
            bin/Release-linux-x86_64/Common
            bin-int/Release-linux-x86_64/Cabrankengine
            bin-int/Release-linux-x86_64/Common

      - name: Mark artifacts as up-to-date
        run: find bin/ bin-int/ -type f 2>/dev/null | xargs -r touch

      - name: Cache Vulkan SDK
        id: vulkan-cache
        uses: actions/cache@v4
        with:
          key: vulkan-sdk-${{ env.VULKAN_SDK_VERSION }}-linux
          path: ./vulkan_sdk

      - name: Install Vulkan SDK
        if: steps.vulkan-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL "https://sdk.lunarg.com/sdk/download/${VULKAN_SDK_VERSION}/linux/vulkansdk-linux-x86_64-${VULKAN_SDK_VERSION}.tar.xz" -o vulkan_sdk.tar.xz
          mkdir -p vulkan_sdk
          tar -xJf vulkan_sdk.tar.xz -C vulkan_sdk --strip-components=1
          rm vulkan_sdk.tar.xz

      - name: Set Vulkan SDK environment
        run: |
          echo "VULKAN_SDK=${GITHUB_WORKSPACE}/vulkan_sdk/x86_64" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${GITHUB_WORKSPACE}/vulkan_sdk/x86_64/lib:${LD_LIBRARY_PATH}" >> "$GITHUB_ENV"

      - name: Build
        if: steps.engine-cache.outputs.cache-hit != 'true'
        run: ./premake5 gmake && make Cabrankengine config=release -j$(nproc)

  # -----------------------------------------------------------------------
  # Sandbox — waits for engine and vendors; skipped engine is treated as success
  # -----------------------------------------------------------------------
  build-sandbox:
    name: Build Sandbox (Release)
    needs: [changes, build-cabrankengine, build-engine-vendors]
    if: |
      needs.changes.outputs.sandbox == 'true' &&
      (needs.build-cabrankengine.result == 'success' || needs.build-cabrankengine.result == 'skipped')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            libx11-dev libxrandr-dev libxi-dev libxxf86vm-dev \
            libxcursor-dev libxinerama-dev libgl1-mesa-dev

      - name: Cache premake5
        id: premake-cache
        uses: actions/cache@v4
        with:
          key: premake5-5.0.0-beta8-linux
          path: ./premake5

      - name: Download premake5
        if: steps.premake-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz | tar -xz -C .
          chmod +x ./premake5

      - name: Restore engine vendor libs
        uses: actions/cache/restore@v4
        with:
          key: engine-vendors-release-linux-${{ hashFiles('Cabrankengine/vendor/GLFW/**', 'Cabrankengine/vendor/glad/**', 'Cabrankengine/vendor/imgui/**', 'Cabrankengine/vendor/freetype/**') }}
          path: |
            bin/Release-linux-x86_64/GLFW
            bin/Release-linux-x86_64/glad
            bin/Release-linux-x86_64/ImGui
            bin/Release-linux-x86_64/FreeType
            bin-int/Release-linux-x86_64/GLFW
            bin-int/Release-linux-x86_64/glad
            bin-int/Release-linux-x86_64/ImGui
            bin-int/Release-linux-x86_64/FreeType

      - name: Restore engine build outputs
        uses: actions/cache/restore@v4
        with:
          key: engine-release-linux-${{ hashFiles('Cabrankengine/src/**', 'Common/**') }}
          path: |
            bin/Release-linux-x86_64/Cabrankengine
            bin/Release-linux-x86_64/Common
            bin-int/Release-linux-x86_64/Cabrankengine
            bin-int/Release-linux-x86_64/Common

      - name: Mark artifacts as up-to-date
        run: find bin/ bin-int/ -type f 2>/dev/null | xargs -r touch

      - name: Cache Vulkan SDK
        id: vulkan-cache
        uses: actions/cache@v4
        with:
          key: vulkan-sdk-${{ env.VULKAN_SDK_VERSION }}-linux
          path: ./vulkan_sdk

      - name: Install Vulkan SDK
        if: steps.vulkan-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL "https://sdk.lunarg.com/sdk/download/${VULKAN_SDK_VERSION}/linux/vulkansdk-linux-x86_64-${VULKAN_SDK_VERSION}.tar.xz" -o vulkan_sdk.tar.xz
          mkdir -p vulkan_sdk
          tar -xJf vulkan_sdk.tar.xz -C vulkan_sdk --strip-components=1
          rm vulkan_sdk.tar.xz

      - name: Set Vulkan SDK environment
        run: |
          echo "VULKAN_SDK=${GITHUB_WORKSPACE}/vulkan_sdk/x86_64" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${GITHUB_WORKSPACE}/vulkan_sdk/x86_64/lib:${LD_LIBRARY_PATH}" >> "$GITHUB_ENV"

      - name: Build
        run: ./premake5 gmake && make Sandbox config=release -j$(nproc)

  # -----------------------------------------------------------------------
  # CBKAssetConverter — depends on Assimp cache
  # -----------------------------------------------------------------------
  build-asset-converter:
    name: Build CBKAssetConverter (Release)
    needs: [changes, build-assimp]
    if: needs.changes.outputs.converter == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y zlib1g-dev

      - name: Cache premake5
        id: premake-cache
        uses: actions/cache@v4
        with:
          key: premake5-5.0.0-beta8-linux
          path: ./premake5

      - name: Download premake5
        if: steps.premake-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz | tar -xz -C .
          chmod +x ./premake5

      - name: Restore Assimp
        uses: actions/cache/restore@v4
        with:
          key: assimp-release-linux-${{ hashFiles('CBKAssetConverter/vendor/assimp/**') }}
          path: |
            bin/Release-linux-x86_64/Assimp
            bin-int/Release-linux-x86_64/Assimp

      - name: Mark artifacts as up-to-date
        run: find bin/ bin-int/ -type f 2>/dev/null | xargs -r touch

      - name: Build
        # Renderer-agnostic: --renderer=opengl avoids the Vulkan SDK requirement.
        run: ./premake5 gmake --renderer=opengl && make CBKAssetConverter config=release -j$(nproc)

  # -----------------------------------------------------------------------
  # UnitTests — waits for engine and vendors; skipped engine is treated as success
  # -----------------------------------------------------------------------
  build-and-test:
    name: Build & Run UnitTests (Release)
    needs: [changes, build-cabrankengine, build-engine-vendors]
    if: |
      needs.changes.outputs.tests == 'true' &&
      (needs.build-cabrankengine.result == 'success' || needs.build-cabrankengine.result == 'skipped')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            libx11-dev libxrandr-dev libxi-dev libxxf86vm-dev \
            libxcursor-dev libxinerama-dev libgl1-mesa-dev

      - name: Cache premake5
        id: premake-cache
        uses: actions/cache@v4
        with:
          key: premake5-5.0.0-beta8-linux
          path: ./premake5

      - name: Download premake5
        if: steps.premake-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz | tar -xz -C .
          chmod +x ./premake5

      - name: Restore engine vendor libs
        uses: actions/cache/restore@v4
        with:
          key: engine-vendors-release-linux-${{ hashFiles('Cabrankengine/vendor/GLFW/**', 'Cabrankengine/vendor/glad/**', 'Cabrankengine/vendor/imgui/**', 'Cabrankengine/vendor/freetype/**') }}
          path: |
            bin/Release-linux-x86_64/GLFW
            bin/Release-linux-x86_64/glad
            bin/Release-linux-x86_64/ImGui
            bin/Release-linux-x86_64/FreeType
            bin-int/Release-linux-x86_64/GLFW
            bin-int/Release-linux-x86_64/glad
            bin-int/Release-linux-x86_64/ImGui
            bin-int/Release-linux-x86_64/FreeType

      - name: Restore engine build outputs
        uses: actions/cache/restore@v4
        with:
          key: engine-release-linux-${{ hashFiles('Cabrankengine/src/**', 'Common/**') }}
          path: |
            bin/Release-linux-x86_64/Cabrankengine
            bin/Release-linux-x86_64/Common
            bin-int/Release-linux-x86_64/Cabrankengine
            bin-int/Release-linux-x86_64/Common

      - name: Mark artifacts as up-to-date
        run: find bin/ bin-int/ -type f 2>/dev/null | xargs -r touch

      - name: Cache Vulkan SDK
        id: vulkan-cache
        uses: actions/cache@v4
        with:
          key: vulkan-sdk-${{ env.VULKAN_SDK_VERSION }}-linux
          path: ./vulkan_sdk

      - name: Install Vulkan SDK
        if: steps.vulkan-cache.outputs.cache-hit != 'true'
        run: |
          curl -sSL "https://sdk.lunarg.com/sdk/download/${VULKAN_SDK_VERSION}/linux/vulkansdk-linux-x86_64-${VULKAN_SDK_VERSION}.tar.xz" -o vulkan_sdk.tar.xz
          mkdir -p vulkan_sdk
          tar -xJf vulkan_sdk.tar.xz -C vulkan_sdk --strip-components=1
          rm vulkan_sdk.tar.xz

      - name: Set Vulkan SDK environment
        run: |
          echo "VULKAN_SDK=${GITHUB_WORKSPACE}/vulkan_sdk/x86_64" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${GITHUB_WORKSPACE}/vulkan_sdk/x86_64/lib:${LD_LIBRARY_PATH}" >> "$GITHUB_ENV"

      - name: Build
        run: ./premake5 gmake && make UnitTests config=release -j$(nproc)

      - name: Run tests
        run: ./bin/Release-linux-x86_64/UnitTests/UnitTests
